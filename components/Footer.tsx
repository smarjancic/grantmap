
export default function Footer() {
  return (
    <footer className="text-sm text-gray-500 text-center py-10 border-t mt-12 bg-white">
      <p>© {new Date().getFullYear()} GrantMap.org — Public grant listings from verified sources.</p>
    </footer>
  );
}
